# Copy to .env (which is gitignored) OR export these in your shell.
# The agent reads the key from the process environment ONLY.
# It is never read from .review.yml, never written to output files, and never logged.

ANTHROPIC_API_KEY=sk-ant-...

# Optional: skip the API entirely and run the deterministic stub reviewer.
# CODEREVIEW_OFFLINE=1
